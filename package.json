{
  "name": "gtfs-rt-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "Visualizzatore web: feed GTFS-RT protobuf → JSON (FeedMessage)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "gtfs-realtime-bindings": "1.1.1"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
